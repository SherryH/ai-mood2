// for computing the sentiment of the journal entries overtime
// This "sentiment" is added later in the project after the DB schema has been defined
// but fear not, we will not break things by creating one more column
// we need to update schema.prisma

const History = () => {
  return <div>History</div>
}

export default History
